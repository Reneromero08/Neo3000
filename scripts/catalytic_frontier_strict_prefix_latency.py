#!/usr/bin/env python3
"""Dedicated neo-exp-0064 entrypoint for a strict 689-token prompt prefix.

The submitted request remains the exact frozen 690-token water branch. All
trial geometry, identity, timing, lifecycle, direct-control, drift, cleanup,
and bounded-scope gates are inherited. Only the saved root boundary changes from
the rejected exact 690-token prompt to its one-token-short strict prefix.
"""
from __future__ import annotations

import catalytic_frontier_single_request_latency as latency


if __name__ == "__main__":
    raise SystemExit(latency.main(root_boundary="strict-prefix"))
